redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

fastapi:
  host: "0.0.0.0"
  port: 8000

scheduler:
  verifier_interval: 300  # 5分钟验证一次
  fetch_interval: 1800    # 30分钟获取一次
  verifier_url: "http://httpbin.org/ip"
  proxy_pool_name: "proxy_pool"
  logger_name: "HighProxyPool"
